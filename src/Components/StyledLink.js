import React from "react";
import React from "react";
// Link is not used if the component returns null.
// Keeping Link import for now in case the component is re-enabled with its previous content.
import { Link } from "@mui/material"; 
// useTheme is no longer imported as it's not used.

const StyledLink = () => {

  return (
   null
  );
};

export default StyledLink;
